[package]
name = "crabbot-shared"
version = "0.1.0"
edition = "2024"
publish = false
description = "Shared data types for Crabbot (runtime + UI)"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
